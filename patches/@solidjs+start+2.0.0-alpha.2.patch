diff --git a/node_modules/@solidjs/start/dist/server/handler.js b/node_modules/@solidjs/start/dist/server/handler.js
index 38b151c..38b1ab4 100644
--- a/node_modules/@solidjs/start/dist/server/handler.js
+++ b/node_modules/@solidjs/start/dist/server/handler.js
@@ -14,7 +14,11 @@ export function createBaseHandler(createPageEvent, fn, options = {}) {
         middleware: middleware.length ? middleware.map(decorateMiddleware) : undefined,
         handler: decorateHandler(async (e) => {
             const event = getRequestEvent();
-            const url = new URL(event.request.url);
+            const requestUrl = event.request.url;
+            const url =
+                requestUrl.startsWith("/") && !requestUrl.startsWith("//")
+                    ? new URL(requestUrl, "http://localhost")
+                    : new URL(requestUrl);
             const pathname = stripBaseUrl(url.pathname);
             if (pathname.startsWith(SERVER_FN_BASE)) {
                 const serverFnResponse = await handleServerFunction(e);
@@ -141,6 +145,9 @@ export async function createPageEvent(ctx) {
     return pageEvent;
 }
 function initFromFlash(ctx) {
+    if (typeof ctx.nativeEvent?.req?.headers?.get !== "function") {
+        return;
+    }
     const flash = getCookie(ctx.nativeEvent, "flash");
     if (!flash)
         return;
