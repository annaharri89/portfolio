
# portfolio
My portfolio showcasing projects I have worked on.

Production `npm run build` emits prerendered HTML under `dist/<path>/index.html` for each route (per-route title, description, canonical, and Open Graph tags). Configure your static host so existing files are served before SPA fallback to `index.html`, or crawlers that fetch `/about` may not receive `dist/about/index.html`.
